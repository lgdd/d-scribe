import { Routes } from '@angular/router';
import { HealthCheckComponent } from './components/health-check.component';

export const routes: Routes = [
  { path: '', component: HealthCheckComponent },
];
