# Scaffold a new Datadog demo project

Create a new microservice demo project instrumented with Datadog.

Use the `dd-scaffold-demo` skill, which will gather requirements (language, deployment model, products, and narrative) and generate the full project with correct architecture, instrumentation, deployment config, traffic generator, and rule templates.
