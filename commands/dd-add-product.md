# Add a Datadog product to the demo

Add a new Datadog product integration to this demo project. Tell me which product to add, or I'll show you the available options.

Use the `dd-add-product` skill to detect the current project setup and apply the correct Agent, application, and deployment configuration changes.
