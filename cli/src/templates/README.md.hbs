# {{projectName}}

A Datadog demo project generated by [d-scribe](https://github.com/lgdd/d-scribe).

**Instrumentation:** `{{instrumentation}}` mode ({{#if (eq instrumentation "datadog")}}Datadog SDK + Datadog Agent{{else if (eq instrumentation "otel")}}OTel SDK + OTel Collector with `datadog` exporter{{else}}Datadog SDK + DDOT Collector{{/if}}).

## Stack

{{#each servicesByBackend}}
- **{{this.label}}**: {{#each this.services}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
{{/each}}
{{#if frontend}}
- **Frontend**: {{frontend.label}}
{{/if}}
- **Traffic**: Locust (synthetic load generator)
- **Observability**: Datadog Agent
{{#if (eq deploy.stack "k8s")}}
- **Orchestration**: Kubernetes
{{else}}
- **Orchestration**: Docker Compose
{{/if}}

## Quick Start

1. Copy and configure environment variables:

   ```bash
   cp .env.example .env
   # Edit .env with your Datadog API key and site
   ```

{{#if (eq deploy.provider "aws")}}
2. Deploy to AWS:

   ```bash
   cd terraform
   terraform init
   terraform apply
   ./deploy.sh
   ```

3. Access the app at the URL shown in Terraform output.
{{else}}
{{#if (eq deploy.stack "compose")}}
2. Start the stack:

   ```bash
   docker compose up -d
   ```

3. Verify services are running:

   ```bash
   curl http://localhost:8080/health
   ```
{{else}}
2. Start Minikube and build images:

   ```bash
   minikube start
   eval $(minikube docker-env)
   docker compose build
   ```

3. Deploy to Kubernetes:

   ```bash
   helm repo add datadog https://helm.datadoghq.com
   helm install datadog datadog/datadog -f k8s/datadog/values.yaml -n {{namespace}} --create-namespace
   kubectl apply -f k8s/ --recursive
   ```

4. Access the app:

   ```bash
   minikube service service-1 -n {{namespace}}
   ```
{{/if}}
{{/if}}

## Available Skills

See `AGENTS.md` for the full project context and available skills.
